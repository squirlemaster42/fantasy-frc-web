FROM alpine:3.19
RUN apk add --no-cache ca-certificates curl
RUN curl -fsSL https://github.com/pressly/goose/releases/download/v3.27.1/goose_linux_x86_64 -o /usr/local/bin/goose && \
    chmod +x /usr/local/bin/goose
COPY migrations /migrations
ENTRYPOINT ["goose", "-dir", "/migrations"]
